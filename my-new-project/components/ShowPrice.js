import React from 'react';
import {View} from 'react-native';
export default function ConvertPrice(price)
{
  let convertPrice = price+"";
  var i = 0;
  while(true)
  {
    var dot = convertPrice.length;
    dot-=3;
    for(var i = convertPrice.length; i > dot)
  }
  return (
    <View>

    </View>
  );
}
